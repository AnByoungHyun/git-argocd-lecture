apiVersion: v1
kind: Service
metadata:
  name: node-app-svc
  namespace: apps
  labels:
    app: node-app
    app.kubernetes.io/name: node-app
spec:
  type: ClusterIP
  selector:
    app: node-app
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
